export function randomDigit(){return Math.floor(Math.random()*8)+1}export function buildCode(h,t,o){return `${h}${t}${o}`}export function lookupHexagram(data,code){return data?.[code]??null}if(typeof document!=="undefined"){const state={hundreds:null,tens:null,ones:null,isDrawing:false,data:null};const places=["hundreds","tens","ones"];const labels={hundreds:"百位",tens:"十位",ones:"個位"};const buttons=Object.fromEntries(places.map(p=>[p,document.querySelector(`#draw-${p}`)]));const reset=document.querySelector("#reset-divination");const codeEl=document.querySelector("#hexagram-code");const result=document.querySelector("#hexagram-result");const sleep=ms=>new Promise(r=>setTimeout(r,ms));async function load(){try{const r=await fetch('/data/divination.json');if(!r.ok)throw new Error(r.status);state.data=await r.json();buttons.hundreds.disabled=false}catch(e){result.hidden=false;result.innerHTML='<h2>卦辭資料暫時無法載入</h2><p>請稍後再試。</p>';console.error(e)}}async function draw(place){if(state.isDrawing||state[place]!==null)return;const i=places.indexOf(place);if(i>0&&state[places[i-1]]===null)return;state.isDrawing=true;Object.values(buttons).forEach(b=>b.disabled=true);const card=document.querySelector(`[data-place="${place}"]`);const cylinder=card.querySelector('.cylinder');const stick=card.querySelector('.drawn-stick');const status=card.querySelector('.draw-status');status.textContent='誠心抽籤中…';cylinder.classList.add('is-shaking');await sleep(1200);cylinder.classList.remove('is-shaking');const n=randomDigit();state[place]=n;stick.textContent=n;stick.classList.add('is-revealed');status.textContent=`${labels[place]}：${n}`;await sleep(600);state.isDrawing=false;if(i<2){buttons[places[i+1]].disabled=false}else{reveal()}}async function reveal(){const code=buildCode(state.hundreds,state.tens,state.ones);codeEl.textContent=code;document.querySelector('.code-card').hidden=false;await sleep(450);const hit=lookupHexagram(state.data,code);result.hidden=false;if(hit){result.innerHTML=`<h2>${hit.name}・${code}</h2><p class="result-text">${hit.text}</p>`}else{result.innerHTML=`<h2>${code}</h2><p>此卦資料尚待校對，請重新占卦或稍後再試。</p>`;console.error('Missing divination code:',code)}reset.hidden=false}function resetAll(){places.forEach(p=>{state[p]=null;const card=document.querySelector(`[data-place="${p}"]`);card.querySelector('.drawn-stick').classList.remove('is-revealed');card.querySelector('.drawn-stick').textContent='';card.querySelector('.draw-status').textContent=''});codeEl.textContent='';document.querySelector('.code-card').hidden=true;result.hidden=true;result.innerHTML='';reset.hidden=true;buttons.hundreds.disabled=!state.data;buttons.tens.disabled=true;buttons.ones.disabled=true}places.forEach(p=>buttons[p]?.addEventListener('click',()=>draw(p)));reset?.addEventListener('click',resetAll);Object.values(buttons).forEach(b=>{if(b)b.disabled=true});load()}